# Чистка после этапов A/B — чек-лист

*Ревизия всего кода 2026-09-06. Проходим поэтапно, каждая группа — отдельный коммит.
Порядок: сначала A (надёжность), потом B (упрощения), потом C (имена/комментарии).*

## A. Надёжность

- [x] **A1. Воркер TG_MTPROTO может умереть молча при старте.**
  `tg_client.session()` при первичном `ensure()` ловит только `NotAuthorized`;
  любая другая ошибка подключения (Telegram/DNS недоступны при старте контейнера)
  убивает задачу воркера, которую никто не await'ит — симптом только «очередь заполнена».
  Фикс: (а) в `session()` ловить при старте `Exception` (ensure() и так переподключается
  на каждой отправке — самолечение уже есть); (б) в `mqtt_dispatcher.main()` gather'ить
  воркеров доставки и cleanup вместе с `run_dispatcher()` — смерть задачи должна
  ронять процесс с traceback'ом.
- [x] **A2. Уведомление в пустоту.** `dispatch_notification` при пустом `_delivery_queues`
  (воркеры не запущены) молча ничего не делает. Guard: `logger.error(...)` + return.
- [x] **A3. `await summary_task` без защиты.** Неожиданное исключение в фоновой задаче
  сводки (например, KeyError на странном конфиге) уронит событие после того, как видео
  уже закодировано. Обернуть в try → `review_metadata = None` + `logger.exception`.

## B. Упрощения

- [ ] **B1. `media_items` с тегами `type`** в `send_frigate_alert` — рудимент старой
  отправки: собираем с тегами, потом фильтруем обратно. Собирать сразу `photos` и `video`.
- [ ] **B2. `_deliver_bot`, одиночное медиа:** `item['type']` совпадает с именем поля
  Bot API (`photo`/`video`) — ветки `field`/`method` схлопываются:
  `method = "sendPhoto" if item['type'] == "photo" else "sendVideo"; data[item['type']] = ...`.
- [ ] **B3. `_mtproto_send_media_group`:** второй `ensure()` (client уже есть у вызывающего —
  передать параметром); guard на пустой список мёртв (форматтер не выдаёт пустых групп).
- [ ] **B4. `_export_record`:** различие `None` (404) / `{}` (ошибка) нигде не используется —
  вернуть `None` в обоих случаях, docstring укоротить.
- [ ] **B5. `os.makedirs(CLIP_DIR)` при импорте** — побочный эффект и избыточно
  (`makedirs(review_dir)` создаёт родителей). Перенести в начало `cleanup_worker`
  (единственный, кому каталог нужен до первого события).
- [ ] **B6. `tg_client.session()`:** ветка `if 'TG_MTPROTO' not in ENABLED_CHANNELS` мертва
  (сессию открывает только воркер этого канала) — убрать вместе с импортом `ENABLED_CHANNELS`.

## C. Имена и комментарии

- [ ] **C1. Устаревшие тексты про владение сессией** (реальность: сессией владеет воркер канала):
  - notifier.py, секция «3. КЛИЕНТ TELETHON» — пустой блок комментария, удалить;
  - tg_client.py — docstring модуля («сессию открывает entry point»);
  - tg_client.py — текст RuntimeError в `ensure()`.
- [ ] **C2. Секции notifier.py по слоям.** «4. Вспомогательные функции» = Frigate API + медиа,
  перемешанные. Переименовать и сгруппировать:
  1 константы · 2 логгер · 3 Frigate API (снапшоты, события, экспорт, сводка/конфиг) ·
  4 медиа (VAAPI, ffmpeg, cleanup) · 5 форматтер · 6 транспорты/каналы/очереди · 7 обработчик.
- [ ] **C3. `mode` → `channel`** во всём слое доставки (переменные остались «режимами»).
- [ ] **C4. Точечные имена:** `detection_times` → `detection_ids` (это id, не времена);
  `p` → `params` в `messenger_style`; `data_or_payload` → `fields` в `_bot_media_fields`;
  `get_time_from_id`/`format_time` — единственные хелперы без `_`, привести к общему стилю.
- [ ] **C5. `send_frigate_alert` → `handle_review`** (обработчик топика, а не отправка);
  затрагивает `tasks_config` и импорт в mqtt_dispatcher, тексты логов в debug-блоке.
- [ ] **C6. mqtt_dispatcher.py:** закомментированный пример в `tasks_config`;
  `maxsize=100` → константа; англо-русская смесь в логах воркера; лишний `{e}`
  в `logger.exception` (traceback уже содержит).

## Осознанно не трогаем

- Два поллинг-цикла в разном стиле (`_wait_export` с backoff, сводка с фиксированным
  шагом) — унификация хелпером не окупается на двух использованиях.
- ffmpeg-блок, форматтер, log_config — в порядке.
- Раскидывание notifier.py по файлам (frigate_api / media / delivery) — отложено до
  появления следующего канала.
